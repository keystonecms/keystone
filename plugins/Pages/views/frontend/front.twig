<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>{% block title %}Keystone CMS{% endblock %}</title>

    <meta name="viewport" content="width=device-width, initial-scale=1">

    {# Bootstrap 5 #}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    {% block styles %}
    <style>
        :root {
            --keystone-primary: #1f3a5f;
            --keystone-secondary: #3c587a;
            --keystone-bg: #f5f7fa;
            --keystone-text: #1c1f24;
            --keystone-muted: #6b7280;
            --keystone-border: #d9dee5;
        }

        body {
            background-color: var(--keystone-bg);
            color: var(--keystone-text);
        }

        .navbar {
            background-color: var(--keystone-primary);
        }

        .navbar-brand,
        .navbar-nav .nav-link {
            color: #fff;
        }

        .navbar-nav .nav-link:hover {
            color: #dce6f2;
        }

        .hero {
            padding: 6rem 1rem;
            background: linear-gradient(
                180deg,
                var(--keystone-primary),
                var(--keystone-secondary)
            );
            color: #fff;
        }

        .hero p {
            max-width: 620px;
            color: #dce6f2;
        }

        .feature-card {
            border: 1px solid var(--keystone-border);
            border-radius: 8px;
            background: #fff;
        }

        .footer {
            background: #fff;
            border-top: 1px solid var(--keystone-border);
            color: var(--keystone-muted);
        }
    </style>
    {% endblock %}
</head>

<body>

{% block navigation %}
<nav class="navbar navbar-expand-lg">
    <div class="container">
        <a class="navbar-brand fw-semibold" href="/">Keystone CMS</a>

        <button class="navbar-toggler text-white"
                type="button"
                data-bs-toggle="collapse"
                data-bs-target="#mainNav">
            ☰
        </button>

        <div class="collapse navbar-collapse" id="mainNav">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item"><a class="nav-link" href="#">Docs</a></li>
                <li class="nav-item"><a class="nav-link" href="#">Plugins</a></li>
                <li class="nav-item"><a class="nav-link" href="#">GitHub</a></li>
                <li class="nav-item">
                    <a class="nav-link fw-semibold" href="/login">Admin</a>
                </li>
            </ul>
        </div>
    </div>
</nav>
{% endblock %}

{% block hero %}{% endblock %}

<main>
    {% block content %}{% endblock %}
</main>

{% block footer %}
<footer class="footer py-4">
    <div class="container d-flex flex-column flex-md-row justify-content-between">
        <span>© {{ "now"|date("Y") }} Keystone CMS</span>
        <span>
            <a href="#" class="text-decoration-none text-muted me-3">Docs</a>
            <a href="#" class="text-decoration-none text-muted">GitHub</a>
        </span>
    </div>
</footer>
{% endblock %}

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

{% block scripts %}{% endblock %}
</body>
</html>
