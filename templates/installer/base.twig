<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Keystone Installer{% endblock %}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <style>
        :root {
            --bg: #0f172a;
            --panel: #111827;
            --border: #1f2933;
            --text: #e5e7eb;
            --muted: #9ca3af;
            --accent: #38bdf8;
            --error: #ef4444;
        }

        * {
            box-sizing: border-box;
            font-family: system-ui, -apple-system, BlinkMacSystemFont, sans-serif;
        }

        body {
            margin: 0;
            background: radial-gradient(circle at top, #020617, var(--bg));
            color: var(--text);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .installer-shell {
            width: 100%;
            max-width: 1100px;
            padding: 2rem;
        }

        .installer-card {
            background: linear-gradient(180deg, #020617, var(--panel));
            border: 1px solid var(--border);
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 30px 80px rgba(0,0,0,.6);
        }

        .installer-header {
            display: flex;
            align-items: center;
            gap: 1rem;
            padding: 1.5rem 2rem;
            border-bottom: 1px solid var(--border);
        }

        .installer-logo {
            width: 36px;
            height: 36px;
            border-radius: 8px;
            background: linear-gradient(135deg, #38bdf8, #6366f1);
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 700;
            color: #020617;
        }

        .installer-header h1 {
            font-size: 1.25rem;
            margin: 0;
            font-weight: 600;
        }

        .installer-body {
            display: grid;
            grid-template-columns: 1.2fr 0.8fr;
            gap: 2rem;
            padding: 2rem;
        }

        @media (max-width: 900px) {
            .installer-body {
                grid-template-columns: 1fr;
            }
        }

        .installer-main {
            padding-right: 1rem;
        }

        .installer-help {
            border-left: 1px solid var(--border);
            padding-left: 2rem;
            color: var(--muted);
            font-size: 0.95rem;
        }

        .installer-help h3 {
            margin-top: 0;
            color: var(--text);
            font-size: 1rem;
        }

        .field {
            margin-bottom: 1.25rem;
        }

        label {
            display: block;
            margin-bottom: .35rem;
            font-size: .9rem;
            color: var(--muted);
        }

        input {
            width: 100%;
            padding: .6rem .7rem;
            border-radius: 6px;
            border: 1px solid var(--border);
            background: #020617;
            color: var(--text);
        }

        input:focus {
            outline: none;
            border-color: var(--accent);
        }

        .actions {
            margin-top: 2rem;
            display: flex;
            justify-content: flex-end;
        }

        button {
            background: linear-gradient(135deg, #38bdf8, #6366f1);
            border: none;
            color: #020617;
            font-weight: 600;
            padding: .6rem 1.2rem;
            border-radius: 6px;
            cursor: pointer;
        }

        .error {
            background: rgba(239,68,68,.1);
            border: 1px solid var(--error);
            color: #fecaca;
            padding: .75rem 1rem;
            border-radius: 6px;
            margin-bottom: 1rem;
        }

        footer {
            text-align: center;
            margin-top: 1.5rem;
            font-size: .8rem;
            color: var(--muted);
        }
.installer-progress {
    margin: 1rem 0;
}

.installer-progress-bar {
    width: 100%;
    height: 8px;
    background: #e5e7eb;
    border-radius: 4px;
    overflow: hidden;
}

#installer-progress-fill {
    height: 100%;
    width: 0%;
    background: #2563eb;
    transition: width 0.3s ease;
}

#installer-progress-label {
    margin-top: 0.5rem;
    font-size: 0.85rem;
    color: #555;
}


    </style>

    {% block head %}{% endblock %}
</head>
<body>

<div class="installer-shell">
    <div class="installer-card">

        <header class="installer-header">
            <div class="installer-logo">K</div>
            <h1>Keystone CMS Installer</h1>
        </header>

        <div class="installer-body">
            <main class="installer-main">
                {% block content %}{% endblock %}
            </main>

            <aside class="installer-help">
                {% block help %}
                    <aside class="wizard-side">
                    <h3 id="wizard-step-title"></h3>
                    <p id="wizard-step-description"></p>
                </aside>
                {% endblock %}
            </aside>
        </div>

    </div>

<div class="installer-progress">
    <div class="installer-progress-bar">
        <div id="installer-progress-fill"></div>
    </div>
    <div id="installer-progress-label"></div>
</div>



    <footer>
        <a href="https://keystone-cms.com">Keystone CMS</a> &middot; Secure by design &copy; 2026
    </footer>
</div>
{% block script %}
<script src="{{url}}/assets/js/installer-ajax.js"></script>
{% endblock %}
</body>
</html>
