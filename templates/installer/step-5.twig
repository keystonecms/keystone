<div class="installer-step installer-finish">

    <h2>Installation complete</h2>

    <p>
        Keystone CMS has been successfully installed.
        Your system is now ready to use.
    </p>

    <ul class="checklist">
        <li>✓ System requirements verified</li>
        <li>✓ Database configured</li>
        <li>✓ Administrator account created</li>
    </ul>

    <hr>

    <h3>Administrator account</h3>

    <p>
        An administrator account has been created automatically.
        For security reasons, a <strong>strong password was generated by the installer</strong>.
    </p>

    <div class="credentials">
        <p>
            <strong>Email</strong><br>
            <code>{{ vars.adminEmail }}</code>
        </p>

        {% if vars.generatedAdminPassword %}
            <p>
                <strong>Temporary password</strong><br>
                <code class="generated-password">
                    {{ vars.generatedAdminPassword }}
                </code>
            </p>

            <div class="notice notice-warning">
                <strong>Important</strong><br>
                This password is shown <strong>only once</strong>.
                Please store it in a safe place.
                You can change it after logging in.
            </div>
        {% else %}
            <div class="notice notice-warning">
                The generated password is no longer available.
                Please reset the password via the admin panel.
            </div>
        {% endif %}
    </div>

    <p class="security-hint" style="margin-top:2rem; opacity:0.8">
        For security reasons, we recommend changing the administrator password
        immediately after logging in and restricting access to the login page if possible.
    </p>

<div class="actions" style="margin-top:2rem">
    <button
        class="btn btn-primary"
        onclick="Installer.completeInstallation(this)"
    >
        Installatie afronden
    </button>
</div>

</div>
